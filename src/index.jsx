import React from 'react';
import ReactDOM from 'react-dom/client'; // Update to React 18 method
import App from './App';

// Create the root for React 18
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
